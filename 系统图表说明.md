# Agent Mesh 系统图表

## 一、实体关系图 (ER Diagram)

### 1. 核心实体关系图

```mermaid
erDiagram
    USER ||--o{ AGENT : creates
    USER ||--o{ MODEL_PROVIDER : configures
    USER ||--o{ KNOWLEDGE_BASE : owns
    USER ||--o{ TOOLS : creates
    USER ||--o{ MCP_SERVERS : owns
    USER ||--o{ CONVERSATION_LOG : has
    USER ||--o{ MODEL_USAGE_COST : tracks
    USER ||--o{ USER_COST_THRESHOLD : sets
    USER ||--o{ WORKFLOW_DEFINITION : creates
    USER ||--o{ TEAM : manages
    
    AGENT ||--o{ AGENT_TOOL_RELATION : uses
    AGENT ||--o{ AGENT_KB_RELATION : links
    AGENT ||--o{ AGENT_DEPENDENCY : depends
    AGENT ||--o{ CONVERSATION_LOG : executes
    AGENT ||--o{ MODEL_USAGE_COST : consumes
    AGENT ||--o{ WORKFLOW_DEFINITION : implements
    
    MODEL_PROVIDER ||--o{ AI_MODEL : provides
    AI_MODEL ||--o{ AGENT : "assigned as decision/response model"
    AI_MODEL ||--o{ CONVERSATION_LOG : "used in execution"
    
    KNOWLEDGE_BASE ||--o{ KNOWLEDGE_BASE_DOCUMENT : contains
    KNOWLEDGE_BASE ||--o{ AGENT_KB_RELATION : linked
    
    TOOLS ||--o{ AGENT_TOOL_RELATION : referenced
    
    TEAM ||--o{ TEAM_RESOURCE_SHARE : shares
    
    USER {
        bigint id PK
        varchar username
        varchar password_hash
        varchar email
        int user_role
        int experience_level
        timestamp created_at
        timestamp updated_at
        int is_delete
        timestamp member_expire_at
    }
    
    AGENT {
        bigint id PK
        bigint user_id FK
        varchar name
        text description
        varchar avatar_url
        text system_prompt
        text role_definition
        bigint decision_model_id FK
        bigint response_model_id FK
        boolean is_tool_enabled
        jsonb tool_schema_json
        varchar version
        int status
        int is_delete
        timestamp created_at
        timestamp updated_at
        int visibility
        jsonb team_ids
        varchar model_selection_strategy
        numeric budget_constraint
    }
    
    MODEL_PROVIDER {
        bigint id PK
        bigint user_id FK
        varchar provider_name
        varchar provider_code
        varchar base_url
        text api_key_encrypted
        text api_secret_encrypted
        int status
        boolean is_public
        int is_delete
        timestamp created_at
        timestamp updated_at
    }
    
    AI_MODEL {
        bigint id PK
        bigint user_id FK
        bigint provider_id FK
        varchar model_name
        varchar model_display_name
        varchar model_type
        int context_window
        int max_tokens
        numeric input_cost_per_1k
        numeric output_cost_per_1k
        varchar currency_type
        boolean is_active
        int is_delete
        timestamp created_at
        timestamp updated_at
    }
    
    KNOWLEDGE_BASE {
        bigint id PK
        bigint user_id FK
        varchar name
        text description
        varchar vector_store_type
        varchar vector_store_table
        bigint embedding_model_id FK
        int chunk_size
        int chunk_overlap
        int status
        int is_delete
        timestamp created_at
        timestamp updated_at
        int visibility
        jsonb team_ids
    }
    
    KNOWLEDGE_BASE_DOCUMENT {
        bigint id PK
        bigint kb_id FK
        varchar doc_name
        varchar doc_type
        varchar source_url
        varchar content_hash
        int chunk_count
        jsonb vector_ids
        jsonb metadata_json
        int status
        int is_delete
        timestamp created_at
        timestamp updated_at
        jsonb related_tool_ids
    }
    
    TOOLS {
        bigint id PK
        bigint user_id FK
        varchar tool_name
        varchar tool_type
        varchar description
        jsonb tool_schema
        varchar http_method
        varchar endpoint_url
        jsonb headers_config
        jsonb request_body_template
        int status
        int is_delete
        timestamp created_at
        timestamp updated_at
    }
    
    MCP_SERVERS {
        bigint id PK
        bigint owner_id FK
        varchar server_name
        varchar transport_type
        varchar endpoint_url
        jsonb command_args
        text auth_config_encrypted
        text env_vars_encrypted
        int status
        timestamp last_heartbeat
        int is_delete
        timestamp created_at
        timestamp updated_at
    }
    
    CONVERSATION_LOG {
        bigint id PK
        bigint user_id FK
        bigint agent_id FK
        varchar session_id
        text user_query
        text final_response
        varchar intent_type
        numeric intent_confidence
        jsonb decision_path
        jsonb invoked_tool_ids
        jsonb searched_kb_ids
        bigint decision_model_id FK
        bigint response_model_id FK
        int total_input_tokens
        int total_output_tokens
        numeric total_cost
        bigint execution_time_ms
        int status
        text error_message
        int user_rating
        text user_feedback
        timestamp created_at
        timestamp updated_at
    }
    
    MODEL_USAGE_COST {
        bigint id PK
        bigint user_id FK
        bigint agent_id FK
        bigint model_id FK
        varchar model_type
        int input_tokens
        int output_tokens
        int total_tokens
        numeric cost
        varchar currency_type
        int status
        timestamp created_at
    }
    
    USER_COST_THRESHOLD {
        bigint id PK
        bigint user_id FK
        bigint agent_id FK
        numeric daily_threshold
        numeric weekly_threshold
        numeric monthly_threshold
        numeric total_threshold
        boolean alert_enabled
        boolean auto_downgrade_enabled
        varchar downgrade_strategy
        bigint target_model_id FK
        varchar notification_method
        varchar notification_target
        timestamp last_alert_time
        int alert_count_today
        int status
        timestamp created_at
        timestamp updated_at
    }
    
    WORKFLOW_DEFINITION {
        bigint id PK
        varchar workflow_name
        text description
        bigint agent_id FK
        varchar version
        jsonb nodes_json
        varchar start_node_id
        jsonb global_variables_json
        bigint timeout_ms
        boolean enabled
        bigint user_id FK
        timestamp created_at
        timestamp updated_at
        int is_delete
    }
    
    TEAM {
        bigint id PK
        varchar name
        varchar description
        bigint owner_id FK
        jsonb member_ids
        timestamp created_at
        timestamp updated_at
        int is_delete
    }
    
    AGENT_TOOL_RELATION {
        bigint id PK
        bigint agent_id FK
        varchar tool_type
        bigint tool_ref_id FK
        jsonb config_params
        int sort_order
        int is_delete
    }
    
    AGENT_KB_RELATION {
        bigint id PK
        bigint agent_id FK
        bigint kb_id FK
        int search_top_k
        numeric similarity_threshold
        int sort_order
        int is_delete
        timestamp created_at
        timestamp updated_at
    }
    
    AGENT_DEPENDENCY {
        bigint id PK
        bigint agent_id FK
        bigint depends_on_agent_id FK
        varchar dependency_type
        int priority
        bigint created_by FK
        timestamp created_at
        timestamp updated_at
    }
    
    TEAM_RESOURCE_SHARE {
        bigint id PK
        bigint team_id FK
        varchar resource_type
        bigint resource_id FK
        int permission_type
        bigint granted_by FK
        timestamp granted_at
    }
```

## 二、用例图 (Use Case Diagram)

### 1. 普通用户用例图

```mermaid
graph TB
    subgraph 普通用户功能
        A[用户登录/注册] --> B[个人中心管理]
        A --> C[智能体管理]
        A --> D[知识库管理]
        A --> E[工具管理]
        A --> F[模型配置]
        A --> G[对话执行]
        A --> H[工作流管理]
        A --> I[成本监控]
        A --> J[技能市场]
        A --> K[长期记忆]
        
        C --> C1[创建智能体]
        C --> C2[编辑智能体]
        C --> C3[删除智能体]
        C --> C4[配置工具]
        C --> C5[配置知识库]
        C --> C6[设置模型]
        C --> C7[发布/停用]
        
        D --> D1[创建知识库]
        D --> D2[上传文档]
        D --> D3[文档处理]
        D --> D4[删除文档]
        
        E --> E1[创建HTTP工具]
        E --> E2[测试工具]
        E --> E3[删除工具]
        
        F --> F1[配置模型提供商]
        F --> F2[添加模型]
        F --> F3[设置成本]
        
        G --> G1[选择智能体]
        G --> G2[输入问题]
        G --> G3[查看决策过程]
        G --> G4[评价回答]
        G --> G5[查看对话历史]
        
        H --> H1[创建工作流]
        H --> H2[编辑节点]
        H --> H3[执行工作流]
        H --> H4[查看执行历史]
        
        I --> I1[设置成本阈值]
        I --> I2[查看使用统计]
        I --> I3[接收告警]
        
        J --> J1[浏览技能包]
        J --> J2[安装技能]
        J --> J3[发布技能]
        J --> J4[评价技能]
        
        K --> K1[查看记忆]
        K --> K2[编辑记忆]
        K --> K3[删除记忆]
    end
```

### 2. 管理员用例图

```mermaid
graph TB
    subgraph 管理员功能
        A[管理员登录] --> B[用户管理]
        A --> C[智能体管理]
        A --> D[知识库管理]
        A --> E[工具管理]
        A --> F[模型管理]
        A --> G[MCP服务管理]
        A --> H[系统监控]
        A --> I[数据统计]
        A --> J[团队管理]
        
        B --> B1[查看用户列表]
        B --> B2[禁用/启用用户]
        B --> B3[设置用户角色]
        B --> B4[查看用户活动]
        
        C --> C1[查看所有智能体]
        C --> C2[审核公开智能体]
        C --> C3[删除违规智能体]
        
        D --> D1[查看所有知识库]
        D --> D2[审核公开知识库]
        
        E --> E1[查看系统工具]
        E --> E2[管理系统工具]
        
        F --> F1[配置公共模型]
        F --> F2[设置模型参数]
        
        G --> G1[查看所有MCP服务]
        G --> G2[管理服务状态]
        
        H --> H1[系统健康检查]
        H --> H2[服务状态监控]
        H --> H3[性能监控]
        
        I --> I1[使用量统计]
        I --> I2[成本分析]
        I --> I3[用户活跃度]
        I --> I4[智能体使用排行]
        
        J --> J1[创建团队]
        J --> J2[管理团队]
        J --> J3[分配资源权限]
    end
```

## 三、系统功能模块图

```mermaid
graph TB
    System[基于SpringBoot+SpringAI的智能体决策引擎系统]
    
    System --> UserModule[用户模块]
    System --> AgentModule[智能体模块]
    System --> KnowledgeModule[知识库模块]
    System --> ToolModule[工具模块]
    System --> ModelModule[模型模块]
    System --> WorkflowModule[工作流模块]
    System --> McpModule[MCP服务模块]
    System --> CostModule[成本监控模块]
    System --> SkillModule[技能市场模块]
    System --> TeamModule[团队协作模块]
    System --> MemoryModule[长期记忆模块]
    System --> AdminModule[系统管理模块]
    
    UserModule --> UM1[用户注册登录]
    UserModule --> UM2[个人中心]
    UserModule --> UM3[角色权限管理]
    UserModule --> UM4[会员管理]
    
    AgentModule --> AG1[智能体创建]
    AgentModule --> AG2[智能体配置]
    AgentModule --> AG3[智能体发布]
    AgentModule --> AG4[智能体执行]
    AgentModule --> AG5[意图识别]
    AgentModule --> AG6[自主决策]
    AgentModule --> AG7[智能体依赖]
    
    KnowledgeModule --> KB1[知识库创建]
    KnowledgeModule --> KB2[文档上传]
    KnowledgeModule --> KB3[文档处理]
    KnowledgeModule --> KB4[向量存储]
    KnowledgeModule --> KB5[RAG检索]
    KnowledgeModule --> KB6[文档管理]
    
    ToolModule --> TL1[系统工具]
    ToolModule --> TL2[HTTP工具]
    ToolModule --> TL3[MCP工具]
    ToolModule --> TL4[工具测试]
    ToolModule --> TL5[工具推荐]
    ToolModule --> TL6[工具调用]
    
    ModelModule --> MD1[模型提供商配置]
    ModelModule --> MD2[模型管理]
    ModelModule --> MD3[模型选择策略]
    ModelModule --> MD4[多模型协同]
    ModelModule --> MD5[模型调用]
    
    WorkflowModule --> WF1[工作流定义]
    WorkflowModule --> WF2[节点配置]
    WorkflowModule --> WF3[工作流执行]
    WorkflowModule --> WF4[执行历史]
    WorkflowModule --> WF5[条件分支]
    WorkflowModule --> WF6[并行执行]
    
    McpModule --> MC1[MCP服务配置]
    McpModule --> MC2[服务连接管理]
    McpModule --> MC3[服务状态监控]
    McpModule --> MC4[工具发现]
    
    CostModule --> CT1[成本统计]
    CostModule --> CT2[阈值配置]
    CostModule --> CT3[告警通知]
    CostModule --> CT4[自动降级]
    CostModule --> CT5[使用量分析]
    
    SkillModule --> SK1[技能包浏览]
    SkillModule --> SK2[技能安装]
    SkillModule --> SK3[技能发布]
    SkillModule --> SK4[技能评价]
    SkillModule --> SK5[技能管理]
    
    TeamModule --> TM1[团队创建]
    TeamModule --> TM2[成员管理]
    TeamModule --> TM3[资源共享]
    TeamModule --> TM4[权限控制]
    
    MemoryModule --> MM1[记忆存储]
    MemoryModule --> MM2[记忆检索]
    MemoryModule --> MM3[记忆更新]
    MemoryModule --> MM4[记忆管理]
    
    AdminModule --> AD1[用户管理]
    AdminModule --> AD2[资源审核]
    AdminModule --> AD3[系统监控]
    AdminModule --> AD4[数据统计]
    AdminModule --> AD5[配置管理]
```

## 四、智能体执行流程图

```mermaid
graph TB
    Start[用户输入问题] --> A[意图识别]
    A --> B{判断意图类型}
    B -->|问答| C[知识库检索]
    B -->|工具调用| D[工具匹配]
    B -->|混合意图| E[综合决策]
    
    C --> C1[RAG向量检索]
    C1 --> C2[知识增强]
    C2 --> F[生成回答]
    
    D --> D1[RAG工具推荐]
    D1 --> D2[工具选择]
    D2 --> D3[工具调用]
    D3 --> D4[结果处理]
    D4 --> F
    
    E --> E1[知识库检索]
    E --> E2[工具调用]
    E1 --> E3[知识增强]
    E2 --> E4[结果整合]
    E3 --> F
    E4 --> F
    
    F --> G{是否需要多轮}
    G -->|是| H[会话管理]
    H --> Start
    G -->|否| I[输出结果]
    
    I --> J[记录对话日志]
    J --> K[更新长期记忆]
    K --> L[统计成本]
    L --> End[完成]
```

## 五、实体详细说明

### 1. 用户实体 (User)
- **主要属性**: id, username, passwordHash, email, userRole, experienceLevel, createdAt, updatedAt
- **角色**: 0=普通用户, 90=管理员, 99=超级管理员
- **关系**: 拥有智能体、知识库、工具、模型配置等

### 2. 智能体实体 (Agent)
- **主要属性**: id, userId, name, description, systemPrompt, decisionModelId, responseModelId, toolSchemaJson, version, status
- **特性**: 支持双模型架构(决策模型+回复模型)、工具配置、版本管理
- **关系**: 关联工具、知识库、模型、依赖关系

### 3. 模型提供商 (ModelProvider)
- **主要属性**: id, userId, providerName, providerCode, baseUrl, apiKeyEncrypted, status
- **关系**: 提供多个AI模型

### 4. AI模型 (AiModel)
- **主要属性**: id, userId, providerId, modelName, modelType, contextWindow, maxTokens, inputCostPer1k, outputCostPer1k
- **类型**: CHAT, EMBEDDING, IMAGE
- **关系**: 归属提供商、被智能体使用

### 5. 知识库 (KnowledgeBase)
- **主要属性**: id, userId, name, description, vectorStoreType, embeddingModelId, chunkSize, chunkOverlap
- **关系**: 包含多个文档、被智能体关联

### 6. 工具 (Tools)
- **主要属性**: id, userId, toolName, toolType, toolSchema, endpointUrl
- **类型**: SYSTEM, USER_HTTP, USER_MCP
- **关系**: 被智能体关联使用

### 7. MCP服务器 (McpServers)
- **主要属性**: id, ownerId, serverName, transportType, endpointUrl, commandArgs
- **传输协议**: SSE, STDIO, STREAMABLE_HTTP

### 8. 对话日志 (ConversationLog)
- **主要属性**: id, userId, agentId, sessionId, userQuery, finalResponse, intentType, decisionPath, totalCost
- **用途**: 记录对话过程、决策路径、成本统计

### 9. 工作流定义 (WorkflowDefinition)
- **主要属性**: id, workflowName, agentId, version, nodesJson, startNodeId, globalVariablesJson
- **特性**: 支持多节点编排、条件分支、并行执行

### 10. 团队 (Team)
- **主要属性**: id, name, description, ownerId, memberIds
- **关系**: 共享资源、权限管理

## 六、数据关系总结

### 核心关联关系
1. **User → Agent**: 一对多，用户创建和管理智能体
2. **Agent → Tools**: 多对多，通过AgentToolRelation关联
3. **Agent → KnowledgeBase**: 多对多，通过AgentKbRelation关联
4. **ModelProvider → AiModel**: 一对多，提供商提供多个模型
5. **Agent → AiModel**: 多对一，智能体配置决策模型和回复模型
6. **KnowledgeBase → KnowledgeBaseDocument**: 一对多，知识库包含多个文档
7. **User → Team**: 一对多，用户创建和管理团队
8. **Team → Resources**: 一对多，团队共享资源

### 数据隔离策略
- **用户级隔离**: 大部分资源通过userId关联，实现多租户数据隔离
- **团队共享**: 通过visibility和teamIds字段控制资源可见性
- **公开资源**: 支持资源公开共享(visibility=2)

### 关键设计特点
1. **逻辑删除**: 所有主要实体支持isDelete字段
2. **版本管理**: Agent和Workflow支持版本控制
3. **成本监控**: 完整的成本统计和阈值告警机制
4. **权限控制**: 用户角色、团队权限、资源可见性三级权限体系
5. **多模型协同**: 支持多个模型提供商和模型选择策略
