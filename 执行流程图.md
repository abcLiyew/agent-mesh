# 智能体决策引擎执行流程图（核心版）

## 1. 统一引擎主执行流程

```mermaid
graph TB
    Start([用户请求]) --> CheckWF{指定工作流?}
    CheckWF -->|是| WFExec[工作流引擎]
    CheckWF -->|否| Intent[意图识别]
    
    Intent --> Complexity{任务复杂度}
    Complexity -->|简单| DirectLLM[直接LLM回答]
    Complexity -->|复杂| PlanExec[AI规划+执行]
    
    PlanExec --> Tools[调用工具/智能体]
    Tools --> Response[生成回答]
    DirectLLM --> Response
    Response --> End([返回结果])
    
    WFExec --> WFEnd([完成])
    
    style Start fill:#e1f5ff
    style End fill:#e6ffe6
    style WFEnd fill:#e6ffe6
    style Complexity fill:#fff4e6
```

## 2. 意图识别流程

```mermaid
graph LR
    Start([用户查询]) --> Build[构建Prompt]
    Build --> CallLLM[调用LLM]
    CallLLM --> Parse{解析成功?}
    
    Parse -->|是| Extract[提取意图/参数]
    Parse -->|否| Fallback[降级匹配]
    
    Extract --> Result([返回结果])
    Fallback --> Result
    
    style Start fill:#e1f5ff
    style Result fill:#e6ffe6
    style CallLLM fill:#fff4e6
```

## 3. 循环依赖检测（DFS）

```mermaid
graph TB
    Start([添加依赖]) --> DFS[DFS遍历]
    DFS --> Check{已访问?}
    
    Check -->|是| Cycle[发现循环]
    Check -->|否| Mark[标记访问]
    
    Mark --> GetNext[获取依赖]
    GetNext --> HasNext{有依赖?}
    
    HasNext -->|是| DFS
    HasNext -->|否| Backtrack[回溯]
    
    Cycle --> Error([失败])
    Backtrack --> Success([成功])
    
    style Start fill:#e1f5ff
    style Error fill:#ffe6e6
    style Success fill:#e6ffe6
    style Cycle fill:#ff6b6b
```

## 4. 自适应模型选择

```mermaid
graph TB
    Start([开始选择]) --> Filter[预算过滤]
    Filter --> Strategy{选择策略}
    
    Strategy -->|按成本| Cost[最便宜]
    Strategy -->|按性能| Perf[最快]
    Strategy -->|按质量| Qual[最高质]
    Strategy -->|自适应| Adapt{评估复杂度}
    
    Adapt -->|简单| Cost
    Adapt -->|复杂| Qual
    
    Cost --> Select[选择模型]
    Perf --> Select
    Qual --> Select
    
    Select --> End([返回模型])
    
    style Start fill:#e1f5ff
    style End fill:#e6ffe6
    style Adapt fill:#fff4e6
```

## 5. 工作流递归执行

```mermaid
graph TB
    Start([执行工作流]) --> Init[初始化]
    Init --> GetNode[获取节点]
    
    GetNode --> Type{节点类型}
    
    Type -->|工具/智能体| Execute[执行]
    Type -->|条件| Branch[分支]
    Type -->|顺序/并行| Loop[循环/并发]
    Type -->|结束| Output[输出结果]
    
    Execute --> Store[存储结果]
    Branch --> Store
    Loop --> Store
    
    Store --> Next{还有节点?}
    Next -->|是| GetNode
    Next -->|否| Complete([完成])
    Output --> Complete
    
    style Start fill:#e1f5ff
    style Complete fill:#e6ffe6
    style Type fill:#fff4e6
    style Loop fill:#d0ebff
```

## 6. RAG检索增强

```mermaid
graph LR
    Start([RAG检索]) --> Vector[向量化]
    Vector --> Search[相似度搜索]
    
    Search --> Filter{达标?}
    Filter -->|是| Keep[保留]
    Filter -->|否| Skip[跳过]
    
    Keep --> Context[构建上下文]
    Skip --> Context
    
    Context --> LLM[调用LLM]
    LLM --> End([返回增强回答])
    
    style Start fill:#e1f5ff
    style End fill:#e6ffe6
    style Search fill:#fff4e6
```

## 7. MCP工具调用

```mermaid
graph LR
    Start([调用MCP]) --> Valid{端点有效?}
    
    Valid -->|否| Error([异常])
    Valid -->|是| Send[发送请求]
    
    Send --> Wait{等待响应}
    Wait -->|超时| Timeout([超时])
    Wait -->|成功| Parse[解析]
    
    Parse --> Result([返回结果])
    
    style Start fill:#e1f5ff
    style Result fill:#e6ffe6
    style Error fill:#ffe6e6
    style Timeout fill:#ffe6e6
```

## 8. 流式执行（SSE）

```mermaid
graph TB
    Start([流式请求]) --> Intent[意图识别]
    Intent --> Simple{简单任务?}
    
    Simple -->|是| StreamLLM[直接流式响应]
    Simple -->|否| Plan[AI规划任务]
    
    Plan --> Push1[推送:规划完成]
    Push1 --> Loop[执行步骤循环]
    
    Loop --> Push2[推送:步骤进度]
    Push2 --> Next{还有步骤?}
    Next -->|是| Loop
    Next -->|否| Push3[推送:生成回答]
    
    Push3 --> GenFinal[生成最终回答]
    GenFinal --> Push4[推送:完成]
    Push4 --> Save[保存日志]
    
    StreamLLM --> Save
    Save --> End([结束])
    
    style Start fill:#e1f5ff
    style End fill:#e6ffe6
    style Plan fill:#fff4e6
    style Push2 fill:#d0ebff
```

---

## 图例说明

| 颜色 | 含义 |
|------|------|
| 🔵 浅蓝 | 开始/结束节点 |
| 🟡 浅黄 | 关键决策点/算法核心 |
| 🟢 浅绿 | 成功/完成状态 |
| 🔴 浅红 | 错误/异常处理 |
| 🔷 浅蓝灰 | 并行/异步操作 |

---

**生成时间**: 2026-04-28  
**适用场景**: 毕业论文答辩、技术文档、系统设计评审

https://agent.qianwen.com/mos/485a77f2352a43799b394bf8d0ec645c/cc893c1b1ed5a42123b6a2438b98db1f
